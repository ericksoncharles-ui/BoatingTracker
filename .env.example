# Read only by the API server (server/index.js), never bundled into the browser.
# Do NOT prefix this with VITE_ — Vite inlines VITE_* vars into the client bundle.
ANTHROPIC_API_KEY=your-api-key-here
PORT=3001
